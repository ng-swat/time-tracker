import { MinutesToHourFormatPipe } from './minutes-to-hour-format.pipe';

describe('MinutesToHourFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MinutesToHourFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
